# Core Dependencies
MetaTrader5>=5.0.45
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
python-telegram-bot[job-queue]>=21.0
python-dotenv>=1.0.0

# Dependencies with Python 3.14+ compatibility notes:
# pandas-ta 0.4.71b0 has a strict dependency on numba==0.61.2.
# On Python 3.14, numba 0.61.2 fails to build. 
# We must use a pre-release version of numba (0.65.1+) and install pandas-ta 
# manually with --no-deps to bypass this conflict.

# numba>=0.65.1; python_version >= "3.14"
# pandas_ta>=0.3.14b
tqdm>=4.67.1
colorama>=0.4.6

# Optional — AutoML Research Pipeline (ml_research/)
# Install manually: pip install optuna lightgbm
# optuna>=3.0.0
# lightgbm>=4.0.0
